## Summary

<!-- What problem does this solve, and what does it change? One or two sentences. -->

## Changes

<!-- Bullet the notable changes. Call out anything that affects distribution artifacts (release.sh / Homebrew cask). -->

-

## Testing

<!-- How was this verified? e.g. `swift test`, `scripts/make-app.sh` + manual run against docs/ui-mock.html -->

- [ ] `swift build` passes
- [ ] `swift test` passes (required for changes in VitermCore / GitKit / VitermServices)
- [ ] Verified with `scripts/make-app.sh` for UI changes

## Notes

<!-- Known limitations, follow-ups, or behavior that reviewers might find surprising. Delete if none. -->
