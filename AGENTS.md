# viterm — agent guidelines

プロジェクト概要・ビルド手順・構成・規約は [CLAUDE.md](CLAUDE.md) を参照(内容はどのエージェントにも共通で適用される)。

特に:

- **コードコメントは英語で書く**(doc comment・`// MARK:` 含む)。UI 文言などの文字列リテラルは日本語のまま
- PR・issue・コミットメッセージ等、リポジトリに残るテキストは英語
