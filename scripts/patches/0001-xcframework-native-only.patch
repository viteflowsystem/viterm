diff --git a/src/build/GhosttyXCFramework.zig b/src/build/GhosttyXCFramework.zig
index 3afeb90..bd9da64 100644
--- a/src/build/GhosttyXCFramework.zig
+++ b/src/build/GhosttyXCFramework.zig
@@ -15,75 +15,92 @@ pub fn init(
     deps: *const SharedDeps,
     target: Target,
 ) !GhosttyXCFramework {
-    // Universal macOS build
-    const macos_universal = try GhosttyLib.initMacOSUniversal(b, deps);
+    // viterm patch (see docs/ghostty-integration.md): only construct the
+    // library variants actually needed for the requested xcframework
+    // target. Upstream unconditionally builds all four variants (macOS
+    // universal, native macOS, iOS, iOS Simulator) regardless of `target`
+    // and only filters which ones are *included in the output* afterwards.
+    // That means even `-Dxcframework-target=native` used to require a
+    // working iOS/iOS Simulator SDK (via the apple-sdk package's
+    // `LibCInstallation.findNative`), which isn't available when using a
+    // Command Line Tools-only developer directory as a workaround for a
+    // Zig 0.15.2 linker/SDK incompatibility. viterm only ever needs the
+    // native macOS slice, so we skip building the others entirely.
+    const xcframework = switch (target) {
+        .universal => xc: {
+            // Universal macOS build
+            const macos_universal = try GhosttyLib.initMacOSUniversal(b, deps);
 
-    // Native macOS build
-    const macos_native = try GhosttyLib.initStatic(b, &try deps.retarget(
-        b,
-        Config.genericMacOSTarget(b, null),
-    ));
+            // iOS
+            const ios = try GhosttyLib.initStatic(b, &try deps.retarget(
+                b,
+                b.resolveTargetQuery(.{
+                    .cpu_arch = .aarch64,
+                    .os_tag = .ios,
+                    .os_version_min = Config.osVersionMin(.ios),
+                    .abi = null,
+                }),
+            ));
 
-    // iOS
-    const ios = try GhosttyLib.initStatic(b, &try deps.retarget(
-        b,
-        b.resolveTargetQuery(.{
-            .cpu_arch = .aarch64,
-            .os_tag = .ios,
-            .os_version_min = Config.osVersionMin(.ios),
-            .abi = null,
-        }),
-    ));
+            // iOS Simulator
+            const ios_sim = try GhosttyLib.initStatic(b, &try deps.retarget(
+                b,
+                b.resolveTargetQuery(.{
+                    .cpu_arch = .aarch64,
+                    .os_tag = .ios,
+                    .os_version_min = Config.osVersionMin(.ios),
+                    .abi = .simulator,
 
-    // iOS Simulator
-    const ios_sim = try GhosttyLib.initStatic(b, &try deps.retarget(
-        b,
-        b.resolveTargetQuery(.{
-            .cpu_arch = .aarch64,
-            .os_tag = .ios,
-            .os_version_min = Config.osVersionMin(.ios),
-            .abi = .simulator,
+                    // We force the Apple CPU model because the simulator
+                    // doesn't support the generic CPU model as of Zig 0.14 due
+                    // to missing "altnzcv" instructions, which is false. This
+                    // surely can't be right but we can fix this if/when we get
+                    // back to running simulator builds.
+                    .cpu_model = .{ .explicit = &std.Target.aarch64.cpu.apple_a17 },
+                }),
+            ));
 
-            // We force the Apple CPU model because the simulator
-            // doesn't support the generic CPU model as of Zig 0.14 due
-            // to missing "altnzcv" instructions, which is false. This
-            // surely can't be right but we can fix this if/when we get
-            // back to running simulator builds.
-            .cpu_model = .{ .explicit = &std.Target.aarch64.cpu.apple_a17 },
-        }),
-    ));
-
-    // The xcframework wraps our ghostty library so that we can link
-    // it to the final app built with Swift.
-    const xcframework = XCFrameworkStep.create(b, .{
-        .name = "GhosttyKit",
-        .out_path = "macos/GhosttyKit.xcframework",
-        .libraries = switch (target) {
-            .universal => &.{
-                .{
-                    .library = macos_universal.output,
-                    .headers = b.path("include"),
-                    .dsym = macos_universal.dsym,
-                },
-                .{
-                    .library = ios.output,
-                    .headers = b.path("include"),
-                    .dsym = ios.dsym,
-                },
-                .{
-                    .library = ios_sim.output,
-                    .headers = b.path("include"),
-                    .dsym = ios_sim.dsym,
+            break :xc XCFrameworkStep.create(b, .{
+                .name = "GhosttyKit",
+                .out_path = "macos/GhosttyKit.xcframework",
+                .libraries = &.{
+                    .{
+                        .library = macos_universal.output,
+                        .headers = b.path("include"),
+                        .dsym = macos_universal.dsym,
+                    },
+                    .{
+                        .library = ios.output,
+                        .headers = b.path("include"),
+                        .dsym = ios.dsym,
+                    },
+                    .{
+                        .library = ios_sim.output,
+                        .headers = b.path("include"),
+                        .dsym = ios_sim.dsym,
+                    },
                 },
-            },
+            });
+        },
+
+        .native => xc: {
+            // Native macOS build
+            const macos_native = try GhosttyLib.initStatic(b, &try deps.retarget(
+                b,
+                Config.genericMacOSTarget(b, null),
+            ));
 
-            .native => &.{.{
-                .library = macos_native.output,
-                .headers = b.path("include"),
-                .dsym = macos_native.dsym,
-            }},
+            break :xc XCFrameworkStep.create(b, .{
+                .name = "GhosttyKit",
+                .out_path = "macos/GhosttyKit.xcframework",
+                .libraries = &.{.{
+                    .library = macos_native.output,
+                    .headers = b.path("include"),
+                    .dsym = macos_native.dsym,
+                }},
+            });
         },
-    });
+    };
 
     return .{
         .xcframework = xcframework,
